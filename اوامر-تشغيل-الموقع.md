# اوامر تشغيل موقع دكتور عمرو الشامي على جهاز تاني

استخدم الخطوات دي على الجهاز اللي هيشتغل 24 ساعة.

## 1. ثبت البرامج المطلوبة

ثبت Node.js 22 LTS أو أحدث:

```text
https://nodejs.org/
```

ثبت Git:

```text
https://git-scm.com/
```

ثبت Cloudflare Tunnel من PowerShell:

```powershell
winget install --id Cloudflare.cloudflared
```

## 2. انسخ المشروع من الفلاشة

انسخ فولدر المشروع لمكان ثابت، مثلا:

```text
C:\Sites\dr-amr-elshamy-site
```

## 3. جهز المشروع

افتح PowerShell جوه فولدر المشروع وشغل:

```powershell
.\01-install-and-build.ps1
```

لو عايز تشغلها يدوي:

```powershell
npm install
npm run build
```

## 4. شغل الموقع

افتح PowerShell جوه فولدر المشروع وشغل:

```powershell
.\02-start-site.ps1
```

سيب النافذة دي مفتوحة. الموقع محليا هيكون:

```text
http://localhost:3000
```

## 5. اعمل لينك للعميل

افتح PowerShell تاني جوه فولدر المشروع وشغل:

```powershell
.\03-start-tunnel.ps1
```

هيظهر لينك بالشكل ده:

```text
https://something.trycloudflare.com
```

ابعت اللينك ده للعميل. لوحة الادمن:

```text
https://something.trycloudflare.com/admin
```

## مهم

- الجهاز لازم يفضل شغال.
- الانترنت لازم يفضل شغال.
- لازم تسيب نافذة تشغيل الموقع ونافذة Cloudflare مفتوحين.
- لينك Cloudflare المجاني بيتغير كل مرة تقفله وتشغله.
- للنسخة النهائية الثابتة، الافضل بعدين VPS أو Cloudflare Tunnel دائم بدومين.
